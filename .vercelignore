# Python worker — runs separately, not on Vercel
agent/

# Dev-only Express wrapper — Vercel uses api/*.js directly
server.js

# Test files (Vercel would otherwise try to deploy these as functions)
api/*.test.js
api/*.spec.js

# Docs + plans
docs/
